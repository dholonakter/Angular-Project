import { DepartmentfilterPipe } from './departmentfilter.pipe';

describe('DepartmentfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
